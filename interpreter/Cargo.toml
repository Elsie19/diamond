[package]
name = "interpreter"
version = "0.1.0"
edition = "2024"

[dependencies]
collect_into_rc_slice = "1.0.0"
enum-as-inner = "0.7.0"
sig_macro = { version = "0.1.0", path = "../sig_macro" }
type-checker = { version = "0.1.0", path = "../type-checker" }
